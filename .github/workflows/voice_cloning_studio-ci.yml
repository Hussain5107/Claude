name: voice_cloning_studio CI

on:
  push:
    paths:
      - "voice_cloning_studio/**"
      - ".github/workflows/voice_cloning_studio-ci.yml"
  pull_request:
    paths:
      - "voice_cloning_studio/**"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: voice_cloning_studio
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install torch/torchaudio (CPU-only wheels -- only chatterbox itself is mocked in tests)
        run: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

      - name: Install remaining dev dependencies
        run: pip install pytest httpx ruff fastapi python-multipart gradio requests numpy pyloudnorm

      - name: Lint
        run: ruff check .

      - name: Run tests
        run: pytest -v
